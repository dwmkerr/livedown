## ADDED Requirements

### Requirement: GitHub icon is the rightmost status bar item
The GitHub icon link SHALL be rendered as the last child element of the status bar, positioned to the right of the connection status indicator.

#### Scenario: Icon appears after status dot
- **WHEN** the status bar is rendered
- **THEN** the GitHub icon link is the last element in the status bar flex row, to the right of the connection status segment
